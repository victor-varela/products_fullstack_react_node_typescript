- Instalamos reactRouterDom en su nueva version (DATA APIS) que agrupa las rutas diferente al proyecto de bebidas npm i react-router-dom. Creamos archivo en src router.tsx-> aca se configura el router. En la documentacion de reactRouterDom dice la sintaxis que hay que usar el BrowserProvider en main.tsx con la prop router para inyectar las rutas de app.
- Usamos el viejo y querido componente Outlet al igual que el proyecto de bebidas y el key index:true para que la app sepa que en el path:'/' es el index, la pagina principal.
- Ahora podemos hacer el diseño en Layout y se va a compartir en Outlet. El flujo es asi: Layout tiene un diseño y lo comparte con Outlet. 
- Agregamos el routing con <Link><Link/> para navegar las views.
- Usamos la nueva funcion de reat para procesar los Formularios. Eso es: usar la funcion action e importar el componente Form de react-roter-dom.7
- Creamos componente Error reutilizable.
- Creamos carpeta services para conectar Front y Back. Dentro de la carpeta van las funciones que ejecutan las acciones de la API.
- Instalamos Valibot -npm i valibot -  para validar los datos para asegurarnos que lo que vamos a enviar cumpla con lo que requerimos antes de enviar a la REST API, como lo habiamos hecho con ZOD, te acuerdas? yo no mucho pero algo me acuerdo.. jeje
- Creamos carpeta Types para definir los schemas a validar con valibot.
- En services creamos y agrupamos las funciones que interactuan con la API y las llamamos en los componentes segun corresponda.
- Instalamos axios npm i axios para comunicar con la REST API
- CORS es un mecanismo de seguridad que impide compartir recursos de origen cruzado.“Un sitio web solo puede hacer peticiones AJAX a su mismo origen, salvo que el servidor de destino lo permita explícitamente.” El front y el back estan en puertos diferentes y luego estaran en dominios diferente cuando deploy y hay que configurar el cors para que se 'permita' el intercambio de datos entre origenes diferentes.

        CORS es un problema de FRONTEND

        Pero se soluciona en el BACKEND


- Instalamos cors en el SERVER- DESDE LA TERMINAL DE SERVER-- npm i cors, npm i -D @types/cors. Cors UNICAMENTE VIVE EN EL SERVER
- En SERVER: Instalamos morgan: Morgan es el “historial automático” de peticiones HTTP de tu API. npm i morgan / npm i -D @types/morgan
- Usamos al igual que habiamos trabajado con ACTION, ahora usamos LOADERS.. es un 'metodo' de react-reactRouterDom para 'cargar' data en un componente, similar como haciamos con useEffect o Zustand o un reducer..
